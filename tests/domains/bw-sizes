% File 'bw-sizes'
% The description of the blocks world in 'bw-serializable' is
%  enhanced by classifying blocks into large and small -- a
%  distinction that doesn't depend on the state

:- include 'bw-serializable'.

:- constants
  large(block)          :: boolean.

:- variables
  B, B1                 :: block.

% a large block cannot be located on top of a small block
constraint loc(B)=B1 & large(B) ->> large(B1).
